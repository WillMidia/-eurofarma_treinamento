name: eurofarma_treinamento
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.1.0
  sqflite: ^2.2.0+4
  path_provider: ^2.0.7
  path: ^1.8.0
  cupertino_icons: ^1.0.2
  firebase_core: ^2.15.0  # Alterado para uma versão compatível
  firebase_auth: ^4.17.0   # Alterado para uma versão compatível
  cloud_firestore: ^4.17.0  # Alterado para uma versão compatível
  firebase_storage: ^11.0.0  # Adicionado conforme solicitado

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/
  uses-material-design: true
